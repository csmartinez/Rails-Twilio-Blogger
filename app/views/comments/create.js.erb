
  // $('#comments').append ('<%= j render(@comment) %>');
  // $('#comment_form').remove();
  // $('#add').show().after('<%= j render ("@comment") %>');

$('.comment_form').remove();
  // $('#add').show().after('<%= j render ("comment") %>');
$('#add').show();
